// node -i -e "$(< repl.js)"

const a = {
    'a': 1,
    'b': 2,
    'c': 3
}

const b = [1,2,3,4,5]

let obj = {
    
}

function fun(sayi){
    b.filter(filterFunction);
}

const mappingFunction = arr => {if (arr[1] > 1) return arr[0]} 
 

const tagData = {
    "van": {
        "2021-01-01 15:40:00": 10
    },
    "vneg": {
        "2021-01-01 15:40:00": 10
    },
    "ss1distv1min" : {
        "2021-01-01 15:40:00": 10
    },
    "ss1distdur" : {
        "2021-01-01 15:40:00": 10
    }
}


for (const data of Object.keys(tagData)) {
    delete tagData[data]    
}

let saturday = new Date();
//saturday.setDate(saturday.getDate() - (saturday.getDay() + 1) % 7);

const myjson = require("./myjson.js");
const moment = require("moment-timezone");



const arr = [1,2,3,4,5];
const removeArr = [1,2];
const filteredArr = arr.filter(i => !removeArr.includes(i));